export const getWidth = el => el.clientWidth;
export const getHeight = el => el.clientHeight;
export const setWidth = (el, width) => { el.style.width = width; };
export const setHeight = (el, height) => { el.style.height = height; };
